{
  "name": "mindshield",
  "version": "1.0.0",
  "description": "MindShield - A mental health tracker and journal",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}